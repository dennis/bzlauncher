- BUG: Buggy ping. If GUI is busy, ping-measurement is off
- look at /usr/share/applications/opera.desktop for bzflag/bzlauncher protocol handler

- Add a Attribute.operator==(wxString token) used by Query ?
- Add a ColumnClass (name of an Attribute)
	- one exists for each column across views
		- it contains info about positioning and if visible on both the list and details
		- and in list, if it the one the list is sorted by
- Put most/all Attribute()'s into a hash
